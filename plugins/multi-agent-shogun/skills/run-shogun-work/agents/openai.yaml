interface:
  display_name: "Multi-Agent Shogun"
  short_description: "Coordinate subagents with period-drama live updates"
  default_prompt: "Use $run-shogun-work to delegate independent workstreams, show concise Sengoku-period drama live council updates, and return one verified result."

policy:
  allow_implicit_invocation: true
